# MCP Server Configuration
MCP_ROOT_PATH=P:\
MCP_AUTH_TOKEN=          # Required — 64-char hex token (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
MCP_PORT=3100
